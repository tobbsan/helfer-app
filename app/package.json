{
  "name": "helper-app",
  "version": "1.0.0",
  "description": "Native Android shell for the HELFER voice companion. Loads the live app from the server and exposes native voice, speech, and reminder permissions to it.",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android",
    "add:android": "npx cap add android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor-community/text-to-speech": "^8.0.2",
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/local-notifications": "^8.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
