{
  "name": "helper-server",
  "version": "1.0.0",
  "description": "Backend for the HELFER voice companion app: serves the web app and proxies Claude API calls so the API key never reaches the device.",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
